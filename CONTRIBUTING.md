# コントリビューションガイド

BotSiteSystemへのご貢献ありがとうございます！

## 貢献方法

### バグ報告

バグを見つけた場合は、以下の情報を含めてIssueを作成してください：

- バグの詳細な説明
- 再現手順
- 期待される動作
- 実際の動作
- 環境情報（OS、Dockerバージョンなど）

### 機能リクエスト

新機能の提案は大歓迎です。Issueを作成して以下を含めてください：

- 機能の説明
- 使用例
- その機能が必要な理由

### プルリクエスト

1. このリポジトリをフォーク
2. フィーチャーブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add: 素晴らしい機能'`)
4. ブランチにプッシュ (`git push origin feature/amazing-feature`)
5. プルリクエストを作成

## 開発ガイドライン

### コーディング規約

#### Python (Django)

- PEP 8 スタイルガイドに従う
- docstringを書く
- テストを書く

```python
def my_function(param1, param2):
    """
    関数の説明をここに書く。

    Args:
        param1: パラメータ1の説明
        param2: パラメータ2の説明

    Returns:
        戻り値の説明
    """
    return result
```

#### JavaScript (React)

- ESLintの設定に従う
- コンポーネントにはPropTypesを定義
- 意味のある変数名を使用

```javascript
/**
 * コンポーネントの説明
 */
function MyComponent({ prop1, prop2 }) {
  // コンポーネントのロジック
  return (
    // JSX
  );
}
```

### コミットメッセージ

コミットメッセージは以下の形式に従ってください：

- `Add:` 新機能追加
- `Fix:` バグ修正
- `Update:` 既存機能の更新
- `Remove:` 機能の削除
- `Refactor:` リファクタリング
- `Docs:` ドキュメントのみの変更
- `Test:` テストの追加・修正

例：
```
Add: ユーザー認証機能を追加
Fix: 商品一覧ページでのエラーを修正
Update: n8nワークフローの設定を更新
```

### テスト

- 新機能には必ずテストを追加
- バグ修正には再現テストを追加
- すべてのテストが通ることを確認

```bash
# バックエンドテスト
cd backend
python manage.py test

# フロントエンドテスト
cd frontend
npm test
```

### ドキュメント

- コードの変更に伴うドキュメントの更新
- 新機能には使用例を含める
- APIの変更はAPI.mdを更新

## プルリクエストのレビュープロセス

1. 自動テストの通過を確認
2. コードレビューを実施
3. 必要に応じて修正を依頼
4. 承認後、mainブランチにマージ

## 質問やサポート

質問がある場合は、Issueを作成するか、Discussionsで質問してください。

## 行動規範

- 敬意を持って接する
- 建設的なフィードバックを提供
- オープンで協力的な姿勢を保つ

## ライセンス

貢献されたコードはMITライセンスの下で公開されます。
